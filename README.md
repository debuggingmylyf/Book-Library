# Book Library Management System

A full-stack book library management system with JWT authentication, built with Node.js/Express backend and React frontend.

## 🚀 Live Demo

- **Backend**: https://book-library-ten-xi.vercel.app/
- **Frontend**: https://book-library-fronted.vercel.app/

## 📋 Features

### Authentication & Security
- JWT-based authentication with access and refresh tokens
- Password hashing with bcrypt
- Protected API endpoints
- Role-based access control (user/admin)

### Book Management
- View all books with pagination
- Search books by name
- Filter books by rent range
- Filter books by category and rent
- Issue books to users
- Return books with automatic rent calculation

### Transaction Management
- View all books issued by a user
- View transactions within a date range
- Calculate total rent generated by a book
- Track book issuance history

### Admin Features
- Admin dashboard with statistics
- User management

## 🛠️ Tech Stack

### Backend
- Node.js & Express
- MongoDB with Mongoose
- JWT for authentication
- Joi for validation
- bcrypt for password hashing

### Frontend
- React 18
- React Router v6
- Axios for API calls
- CSS with CSS Variables

## 📦 Installation

### Prerequisites
- Node.js (v14 or higher)
- MongoDB (local or Atlas)

### Backend Setup

```bash
cd Backend
npm install
```

Create a `.env` file in the Backend directory:

```env
MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/bookstore
JWT_SECRET=your_jwt_secret_key_here
PORT=8080
FRONTEND_URL=http://localhost:3000
REFRESH_TOKEN_EXPIRY=7
```

Seed the database:

```bash
npm run seed
```

Start the server:

```bash
npm start
```

### Frontend Setup

```bash
cd Frontend
npm install
```

Create a `.env.local` file:

```env
REACT_APP_API_URL=http://localhost:8080/api
```

Start the development server:

```bash
npm start
```

## 🔐 Test Users

After running the seed script, you can use these accounts:

| Email | Password | Role |
|-------|----------|------|
| john.doe@example.com | password123 | user |
| jane.smith@example.com | password123 | user |
| admin@example.com | password123 | admin |

## 📡 API Endpoints

### Authentication
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/auth/register` | Register new user |
| POST | `/api/auth/login` | Login user |
| POST | `/api/auth/refresh-token` | Refresh access token |
| GET | `/api/auth/me` | Get current user profile |

### Books
| Method | Endpoint | Description |
|--------|----------|-------------|
| GET | `/api/books/all` | Get all books (paginated) |
| GET | `/api/books/books` | Get all books (simple list) |
| GET | `/api/books/search?name=<term>` | Search books by name |
| GET | `/api/books/filter-by-rent?minRent=<min>&maxRent=<max>` | Filter by rent range |
| GET | `/api/books/filter-by-category-and-rent` | Filter by category and rent |
| GET | `/api/books/users` | Get all users |

### Transactions (Require Authentication)
| Method | Endpoint | Description |
|--------|----------|-------------|
| POST | `/api/books/issue-book` | Issue a book |
| POST | `/api/books/return-book` | Return a book |
| GET | `/api/books/issued-books?userName=<name>` | Get books issued by user |
| GET | `/api/books/issued-users?bookName=<name>` | Get users who issued a book |
| GET | `/api/books/total-rent?bookName=<name>` | Get total rent for a book |
| GET | `/api/books/issued-in-date-range?startDate=&endDate=` | Get transactions by date range |

## 📝 API Response Format

All API responses follow a standardized format:

### Success Response
```json
{
  "success": true,
  "message": "Operation completed successfully",
  "data": {
    // Response data here
  }
}
```

### Error Response
```json
{
  "success": false,
  "message": "Error description",
  "errors": ["Detailed error messages"]
}
```

### Pagination Response
```json
{
  "success": true,
  "message": "Books fetched successfully",
  "data": {
    "books": [...],
    "totalCount": 50,
    "page": 1,
    "limit": 10,
    "totalPages": 5
  }
}
```

## 🔧 Scripts

### Backend
```bash
npm start          # Start server with nodemon
npm run seed       # Seed database with sample data
```

### Frontend
```bash
npm start          # Start development server
npm run build      # Build for production
npm test           # Run tests
```

## 📁 Project Structure

```
Book-Library/
├── Backend/
│   ├── middleware/
│   │   └── auth.js           # JWT authentication middleware
│   ├── models/
│   │   ├── Book.js           # Book schema
│   │   ├── User.js           # User schema
│   │   └── Transaction.js    # Transaction schema
│   ├── routes/
│   │   └── bookRoutes.js     # API routes
│   ├── scripts/
│   │   └── seed.js           # Database seeding script
│   ├── utils/
│   │   └── response.js       # Response helpers
│   ├── validators/
│   │   └── schemas.js        # Joi validation schemas
│   ├── app.js                # Express app
│   └── package.json
│
├── Frontend/
│   ├── src/
│   │   ├── components/
│   │   │   ├── Admin.js
│   │   │   ├── BookList.js
│   │   │   ├── BookSearch.js
│   │   │   ├── Dashboard.js
│   │   │   ├── ErrorBoundary.js
│   │   │   ├── IssueBook.js
│   │   │   ├── Login.js
│   │   │   ├── Navbar.js
│   │   │   ├── ProtectedRoute.js
│   │   │   ├── Register.js
│   │   │   ├── ReturnBook.js
│   │   │   ├── Transactions.js
│   │   │   └── UserList.js
│   │   ├── context/
│   │   │   └── AuthContext.js
│   │   ├── styles/
│   │   │   └── Auth.css
│   │   ├── api.js
│   │   ├── App.js
│   │   ├── App.css
│   │   └── index.js
│   └── package.json
│
└── README.md
```

## 🌐 Deployment

### Backend (Vercel)
1. Connect your GitHub repository to Vercel
2. Set environment variables in Vercel dashboard
3. Deploy

### Frontend (Vercel)
1. Connect your GitHub repository to Vercel
2. Set `REACT_APP_API_URL` to your backend URL
3. Deploy

## 📄 License

ISC

## 👨‍💻 Author

Created as part of a coding assessment.
